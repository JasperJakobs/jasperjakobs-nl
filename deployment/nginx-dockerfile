FROM nginx:latest

RUN apt-get update && apt-get install -y \
    npm

RUN npm run production

COPY nginx.conf /etc/nginx/conf.d
COPY --from=api /var/www/html/public /usr/share/nginx/html

RUN mkdir /var/www/html -p && touch /var/www/html/index.php